Calibrate the token usage tracker with real data from claude.ai/settings/usage.

Run this command and show the user the output:

```bash
python3 ~/.claude/hooks/token-tracker.py calibrate $ARGUMENTS
```

After running, briefly confirm the calibration results to the user.
